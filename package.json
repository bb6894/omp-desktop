{
  "name": "omp-desktop",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "tauri": "tauri",
    "host:install": "bun install --cwd apps/desktop-host",
    "host:test": "bun test apps/desktop-host/tests",
    "host:build": "bun run tools/build-host.ts"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
